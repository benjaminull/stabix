{
    email {$CADDY_EMAIL}
}

stabix.app {
    # API endpoints
    handle /api/* {
        reverse_proxy api:8000
    }

    # Django admin
    handle /admin/* {
        reverse_proxy api:8000
    }

    # WebSocket
    handle /ws/* {
        reverse_proxy api:8000
    }

    # Static files
    handle /static/* {
        root * /app/staticfiles
        file_server
    }

    # Media files
    handle /media/* {
        root * /app/media
        file_server
    }

    # Frontend (catch-all)
    handle {
        reverse_proxy frontend:3000
    }

    log {
        output stdout
    }
}
